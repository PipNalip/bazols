Run a read-only health check.

1. Read `AGENTS.md` and use its exact commands.
2. Run tests, lint, and the template self-check when present.
3. Start the application or exercise its primary entry point when safe and practical.
4. Verify `.env` is ignored and no secret-like file is tracked; never print secret values.
5. Check README paths and run commands against the current tree.
6. Report: working, broken, not checked, and the smallest next action.
