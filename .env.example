# Public example only. Never put real keys or passwords in this file.
APP_ENV=development
PORT=8000

# Add only variables the application actually reads, using fake values.
# DATABASE_URL=postgresql://user:password@localhost:5432/app
# AI_API_KEY=replace-with-a-fake-value
